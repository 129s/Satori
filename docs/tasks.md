# 任务与里程碑

## M1: KS 最小原型 (当前)
1. 建立基础工程结构（CMake + src/main.cpp）
2. 实现 KarplusStrongString（可设置频率、阻尼、持续时间）
3. 实现 WaveWriter，输出 16-bit PCM WAV
4. 在 main 中串起流程，支持简单命令行参数
5. 跑通单弦测试（如 A4=440Hz，时长 2 秒）并验证输出

## M2: 声音设计扩展
1. 可插拔滤波器链（起码一个低通）
2. 弦模型参数化（拾音点、噪声类型）
3. 增加多复音调度，允许同时触发多根弦

## M3: 环境与交互
1. 加入卷积混响（可读取 IR 文件）
2. 增加键盘/简单 MIDI 输入到触发层
3. 打包示范预设与文档

## 待办池
- 音频参数配置文件（YAML/JSON）
- 自动化测试：波形能量/时长校验
- 引入 CI，构建并上传演示 WAV
